# RPG_Common

Server 与 Client **共用**的客户端 wire 协议头。详见 [RPG_Common](https://github.com/hechuangguo/RPG_Common) 仓库 README。

本目录在 RPG_Server 中为 Git Submodule（路径 `common/`）。

```bash
git submodule update --init --recursive
```
